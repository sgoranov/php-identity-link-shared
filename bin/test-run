#!/usr/bin/env bash
set -euo pipefail

docker compose run --rm \
    --env COMPOSER_HOME=/tmp/composer \
    php composer install --no-interaction --prefer-dist --no-progress

docker compose run --rm \
    php composer test
